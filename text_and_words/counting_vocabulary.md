## Counting Vocabulary

#### Length

Counting words and punctuation symbols with `len`

```python
len(text3)
```

#### Sort text

Use `set` term to list up all distinct words

```python
sorted(set(text3))
len(set(text3))
```
